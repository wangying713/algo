<?php
/**
 * 插入排序
 *
 * @param [type] $nums
 * @return void
 */
function insertSort(&$nums)
{
    $count = count($nums);

    $p = 0;
    for ($i = 0; $i < $count; $i++) {

        for ($j = 0; $j < $count; $j++) {

        }
    }
}
